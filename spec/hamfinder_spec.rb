require "spec_helper"

describe Hamfinder do
  it "has a version number" do
    expect(Hamfinder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
